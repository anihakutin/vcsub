<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <div class="text-center mb-12">
    <h2 class="text-3xl font-bold text-gray-900">Request Items</h2>
    <p class="mt-2 text-gray-600">One founder's trash is another founder's treasure</p>
  </div>

  <% if current_user.company_name.blank? %>
    <div class="max-w-2xl mx-auto bg-yellow-50 border border-yellow-200 rounded-lg p-6 mb-8 text-center">
      <p class="text-yellow-800 mb-4">Please complete your company profile before creating a request</p>
      <%= link_to "Complete Your Profile", edit_profile_path, 
          class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700" %>
    </div>
  <% end %>

  <div class="bg-white rounded-lg shadow-lg overflow-hidden">
    <div class="p-8">
      <%= render "form", request: @request %>
    </div>
  </div>
</div> 